// {Object.keys(this.state.gameBoard).map(function(rowArray) {
//   for(let i=0; i<rowArray.length; i++) {
//     return <Tile spritePath={rowArray[i].spritePath}/>
//   }

//
// {for (let i=0; i<this.state.gameBoard.length; i++) {
//   for (let j=0; j<this.state.gameBoard.length; j++) {
//     return <Tile spritePath={gameBoard[i][j].spritePath}/>
//   }
// }
// }

//
// const getAllTiles = function(){
//   let allTiles = [];
//   let length = this.state.gameBoard.length;
//   for (let i=0; i<length; i++) {
//     let gridRow = []
//     for (let j=0; j<length; j++) {
//       allTiles.push(this.state.gameBoard[i][j])
//     }
//   }
//   return allTiles;
// }

// {Object.keys(this.state.tileArray).map(function(tileKey) {
//   let tile = this.state.tileArray[tileKey];
//   return <Tile spritePath={tile.spritePath} />;
// })}
